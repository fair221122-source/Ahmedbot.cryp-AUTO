app = "ahmedbot-cryp-auto"
primary_region = "iad"

[build]

[env]
  PORT = "8080"

[processes]
  app = "uvicorn bot:app --host 0.0.0.0 --port 8080"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 80

  [[services.ports]]
    port = 443
